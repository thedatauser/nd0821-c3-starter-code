name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.13
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r starter/requirements.txt

      - name: Run tests
        run: pytest tests/test_model.py tests/test_api.py -q

      - name: Run lint
        run: flake8 starter/main.py starter/starter/ml/model.py tests/test_model.py tests/test_api.py
